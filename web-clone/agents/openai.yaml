interface:
  display_name: "Web Clone"
  short_description: "Clone one public page into a Vite React app"
  default_prompt: "Use $web-clone to clone https://www.example.com/ into a local Vite React Tailwind app."
